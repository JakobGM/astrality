config/astrality:
    hot_reload_config: true

module/A:
    on_startup:
        compile:
            - template: templates/no_context.template
              target: /tmp/astrality/target1

    on_exit:
        run:
            - rm {templates/no_context.template}
