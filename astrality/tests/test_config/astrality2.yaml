settings/astrality:
    hot_reload: true

module/A:
    on_startup:
        compile:
            - template: templates/no_context.template
              target: /tmp/astrality/target2

    on_exit:
        run:
            - rm {templates/no_context.template}
